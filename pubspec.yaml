name: key_listener
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.0-0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  window_manager:

flutter:
  uses-material-design: true
